{
  "name": "multi-post",
  "private": true,
  "version": "1.0.0",
  "description": "Run backend and mobile app together",
  "scripts": {
    "start": "node scripts/start-all.js",
    "start:backend": "npm --prefix backend run start",
    "start:mobile": "npm --prefix mobile-app run start"
  }
}
