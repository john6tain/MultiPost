{
  "name": "multi-post",
  "private": true,
  "version": "1.0.0",
  "description": "Mobile app and extension only",
  "scripts": {
    "start": "npm --prefix mobile-app run start",
    "start:mobile": "npm --prefix mobile-app run start"
  }
}
